import Image from 'next/image';
import { FC } from 'react';

const SecurityBar: FC = () => {
  return <Image src={'/img/charts/pos-glob-1.svg'} width={296} height={156} alt="tem chart 1" className="w-72" />;
};

export default SecurityBar;
